import Snapshot from './pages/Snapshot';
import __Layout from './Layout.jsx';


export const PAGES = {
    "Snapshot": Snapshot,
}

export const pagesConfig = {
    mainPage: "Snapshot",
    Pages: PAGES,
    Layout: __Layout,
};